services:
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: front_thundera
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true
    tty: true
